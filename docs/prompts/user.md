# Populate Existing User Avatar Circles

Before making changes, follow all existing project protocols and read the relevant documentation in `/docs`.

## Task

Fill the **existing user avatar circles** in:

- The navbar
- The waitlist section

Use the provided user images:

- `user-1.jpg`
- `user-2.jpg`
- `user-3.jpg`
- `user-4.jpg`
- `user-5.jpg`

Map them sequentially across the existing avatar containers.

## Strict Constraints

Do **not**:

- Create new avatar containers.
- Change the number of avatars.
- Change their size.
- Change their position.
- Change spacing.
- Change borders, radius, or styling.
- Change the layout.
- Change responsive behaviour.
- Change any surrounding text or UI.
- Modify any other part of the landing page.

Simply replace the existing empty/avatar placeholders with the corresponding images.

Preserve the existing avatar stacking/overlap behaviour.

Use proper image handling and ensure the images remain cropped correctly inside the existing circular containers.

After implementation, verify the project builds successfully and update `docs/progress.md`.