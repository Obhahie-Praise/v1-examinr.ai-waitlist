    # Footer Social Icons

Before making changes, read and follow all existing project protocols and documentation in `/docs`.

## Task

Install the **Simple Icons** package using `pnpm`.

Learn and understand the package's API and usage before implementing it.

Replace the text-based social links in the landing page footer with the appropriate official brand icons from Simple Icons.

Use the correct icons for each existing social platform.

### Important

- Do not change the footer layout.
- Do not change spacing.
- Do not change responsive behaviour.
- Do not change the existing links or destinations.
- Do not add or remove social platforms.
- Preserve the existing hover and interaction behaviour where applicable.
- Keep the icons visually consistent with the existing footer design.
- Make sure icons remain accessible with appropriate labels/tooltips where necessary.

This is a **small UI refactor only**. Do not modify anything outside the footer that is unrelated to this task.

After implementation, verify the project builds correctly and update `docs/progress.md`.